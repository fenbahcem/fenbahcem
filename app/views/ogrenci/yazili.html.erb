<style>
    .glyphicon { margin-right:10px;}
    .section-box h2 { margin-top:0px;}
    .section-box h2 a { font-size:15px; }
    .glyphicon-heart { color:#e74c3c;}
    .glyphicon-comment { color:#27ae60;}
    .separator { padding-right:5px;padding-left:5px; }
    .section-box hr {margin-top: 0;margin-bottom: 5px;border: 0;border-top: 1px solid rgb(199, 199, 199);}
</style>

<div class="col-md-12">
<h3 style="margin-top:0px"><%= @unitesinif %>. Sınıf Fen Bilimleri Üniteleri Yazılılar</h3>
<% @unite.each_with_index do |u, i|  %>
<div class="well well-sm">
	<div class="row">
        <div class="col-xs-3 col-md-3 text-center">
<!--					<img src="http://via.placeholder.com/180x114" alt="bootsnipp" class="img-rounded img-responsive" /> -->
					<%= image_tag u.uniteresmi.url(:thumb), class: "img-rounded img-responsive" %>
        </div>
        <div class="col-xs-9 col-md-9 section-box">
            <h4>
                <%= u.uniteadi %>
						</h4>
						Yazılıları görüntülemek için üzerine tıklayınız.
						<table class="table table-striped">
							<% @yazili.each do |d|  %>
								<% if d.uniteadi == u.uniteadi %>
									<tr>
										<td><%= d.materyaladi %>: <%= d.materyalaciklama.html_safe %><br><%= link_to "Yazılı Görüntüle", d.dosya.url %></td>
									</tr>
								<% end  %>
							<% end %>
						</table>
            <hr />
            <div class="row rating-desc">
                <div class="col-md-12">
                    <span class="glyphicon glyphicon-heart"></span>(36)<span class="separator">|</span>
										<span class="glyphicon glyphicon-comment"></span>(100)
                </div>
            </div>
				</div>
    </div>
</div>
<% end %>
</div>
